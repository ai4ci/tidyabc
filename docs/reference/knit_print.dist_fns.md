# Print a `dist_fns` S3 object in an Rd document

Boilerplate S3 class operation

## Usage

``` r
knit_print.dist_fns(x, ...)
```

## Arguments

- x:

  a `dist_fns` S3 object

- ...:

  passed onto methods

## Value

an `as-is` knitr chunk
