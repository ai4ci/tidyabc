# Concatenate or construct a `dist_fns_list`

Concatenate or construct a `dist_fns_list`

## Usage

``` r
# S3 method for class 'dist_fns_list'
c(...)
```

## Arguments

- ...:

  some of `dist_fns_list` and `dist_fns` or list of `dist_fns`s

## Value

a flattened `dist_fns_list` S3 object
