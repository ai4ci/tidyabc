# Concatenate or construct a `link_fns_list`

Concatenate or construct a `link_fns_list`

## Usage

``` r
# S3 method for class 'link_fns_list'
c(...)
```

## Arguments

- ...:

  some of `link_fns_list` and `link_fns` or list of `link_fns`s

## Value

a flattened `link_fns_list` S3 object
