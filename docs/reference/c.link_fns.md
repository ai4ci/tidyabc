# Concatenate a `link_fns` S3 object or `link_fns_list`s

Concatenate a `link_fns` S3 object or `link_fns_list`s

## Usage

``` r
# S3 method for class 'link_fns'
c(...)
```

## Arguments

- ...:

  some of `link_fns`, `link_fns_list`s or lists of `link_fns`

## Value

a `link_fns_list`
