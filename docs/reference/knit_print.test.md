# Print a `test` S3 object in an Rd document

Boilerplate S3 class operation

## Usage

``` r
knit_print.test(x, ...)
```

## Arguments

- x:

  a `test` S3 object

- ...:

  passed onto methods

## Value

an `as-is` knitr chunk
