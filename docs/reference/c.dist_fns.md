# Concatenate a `dist_fns` S3 object or `dist_fns_list`s

Concatenate a `dist_fns` S3 object or `dist_fns_list`s

## Usage

``` r
# S3 method for class 'dist_fns'
c(...)
```

## Arguments

- ...:

  some of `dist_fns`, `dist_fns_list`s or lists of `dist_fns`

## Value

a `dist_fns_list`
