# Concatenate or construct a `test_list`

Concatenate or construct a `test_list`

## Usage

``` r
# S3 method for class 'test_list'
c(...)
```

## Arguments

- ...:

  some of `test_list` and `test` or list of `test`s

## Value

a flattened `test_list` S3 object
