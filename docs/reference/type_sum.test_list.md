# Pillar type name

These boilerplate functions allow generic list behaviour from
`test_list` classes allowing `test` S3 objects to be used in lists or
dataframes.

## Usage

``` r
type_sum.test_list(x, ...)
```

## Arguments

- x:

  a `test_list` S3 object

- ...:

  passed onto methods

## Value

a name
