# Support for auto suggests on `crate`s

Support for auto suggests on `crate`s

## Usage

``` r
# S3 method for class 'crate'
.DollarNames(x, pattern)
```

## Arguments

- x:

  a crated function

- pattern:

  a regular expression

## Value

the names of crated data items
