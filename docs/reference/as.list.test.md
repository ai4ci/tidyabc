# Convert a `test` S3 object into a plain list

Boilerplate S3 class operation

## Usage

``` r
# S3 method for class 'test'
as.list(x, ...)
```

## Arguments

- x:

  a `test` S3 object

- ...:

  passed onto methods

## Value

the internal structure of the object as a plain list
