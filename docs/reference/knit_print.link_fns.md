# Print a `link_fns` S3 object in an Rd document

Boilerplate S3 class operation

## Usage

``` r
knit_print.link_fns(x, ...)
```

## Arguments

- x:

  a `link_fns` S3 object

- ...:

  passed onto methods

## Value

an `as-is` knitr chunk
