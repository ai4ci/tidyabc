# Cast to a list of `test` S3 objects

This function wraps `test` and unwraps plain lists such that the result
is a flat `test_list` containing `test` objects only

## Usage

``` r
as.test_list(x)
```

## Arguments

- x:

  a `test_list` S3 object

## Value

a `test_list` S3 object
