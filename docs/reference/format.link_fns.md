# Format a `link_fns` S3 object

Format a `link_fns` S3 object

## Usage

``` r
# S3 method for class 'link_fns'
format(x, ...)
```

## Arguments

- x:

  a `link_fns` S3 object

- ...:

  passed onto methods

## Value

a character value
