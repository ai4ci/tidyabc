# Concatenate a `test` S3 object or `test_list`s

Concatenate a `test` S3 object or `test_list`s

## Usage

``` r
# S3 method for class 'test'
c(...)
```

## Arguments

- ...:

  some of `test`, `test_list`s or lists of `test`

## Value

a `test_list`
